// Pipeline anidado — tipos del módulo (el estado es derivado/computado, no persistido)
export type {
  Need, NeedKind, Capability, Match, PipelineHealth, FrsTarget, FrsRoute,
} from '@core/lib/pipeline'
