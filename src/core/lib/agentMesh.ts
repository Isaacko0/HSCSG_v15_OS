// HSCSG v15 OS — Lógica agentMesh (block/buzz asimilado)
import { makeAgentMeshState, spawnAgent, shareCompute, requestCompute, remoteResurrect, auditTrail } from '@core/state/agentMesh'

export {
  makeAgentMeshState, spawnAgent, shareCompute, requestCompute, remoteResurrect, auditTrail,
}

export type { Agent } from '@core/state/agentMesh'
